---
alwaysApply: true
---

const handleHerbClick = (item) => {
  if (item.isHerb) {
    // 这里的 name: 'HerbDetail' 必须和你 router/index.js 里的名字一致
    // params 传的是 name (汉字)
    router.push({ name: 'HerbDetail', params: { name: item.name } })
  }
};
